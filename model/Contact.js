const mongoose = require('mongoose')

const {Schema} = mongoose

const contactSchema = newSchema ({

    name: {
        type: String
        required: true,
    },

    email:{},

    phone: Number 





})